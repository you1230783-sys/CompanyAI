; WinRAR 標準 GUI SFX 設定；只解壓縮，不執行任何安裝或啟動指令。
Path=C:\largan\LM_AI
Title=LM_AI 0.8.8 自解壓縮測試包
Overwrite=1
Text
{
本測試包只解壓縮 LM_AI.exe，預設放在 C:\largan\LM_AI。

請先從系統托盤選擇「離開」，完全關閉 LM_AI，再按「解壓縮」。
若目標資料夾已有 LM_AI.exe，將替換為本包版本 0.8.8。
machines.json、user_config.json 與其他既有檔案不會隨包寫入。

解壓縮完成後，請自行開啟資料夾中的 LM_AI.exe。
本包不含 WebView2；缺少時請先安裝公司另外提供的 WebView2 x64。
本包不建立捷徑或解除安裝項目，也不接入自動更新安裝流程。
}
